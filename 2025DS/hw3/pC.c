#include <stdio.h>


// first we handle the 
static int solve(int m, int n, int maze[][]) {
    // make the teleport hole mapping first, view each as node and edge
    
    // use bidirectionalBFS
    return 0;
}

int main() {
    return 0;
}